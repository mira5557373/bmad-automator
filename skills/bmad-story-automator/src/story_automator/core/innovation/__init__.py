from __future__ import annotations

"""Innovation moats — opt-in, additive subsystems layered above the production gate.

Modules in this package are deliberately self-contained and do not depend on
optional gate subsystems. Each is independently importable and exercised by its
own test module under tests/.
"""
