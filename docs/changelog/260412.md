# Changelog - 260412

## 260412-02:41:53 - [LITE] Migrate story automator installer to pure skill layout

### Summary
Moved story-automator installs, runtime packaging, and dependency resolution to the pure `.claude/skills` layout.

### Added
- Added the main `bmad-story-automator` `SKILL.md` entrypoint for pure BMAD installs.
- Added a new changelog entry for the pure-skill migration.

### Changed
- Changed installer output from `_bmad/bmm/...` workflow roots to `.claude/skills/bmad-story-automator` and `.claude/skills/bmad-story-automator-review`.
- Moved the packaged Python helper wrapper to `scripts/story-automator` inside the installed skill.
- Updated runtime workflow resolution and tmux prompt generation to use `.claude/skills` dependency skills and natural-language skill prompts.
- Updated bundled payload references, stop-hook docs, smoke tests, README, contributing docs, and package metadata for the pure skill layout.

### Removed
- Removed command-wrapper generation as the primary install interface for story-automator.

### Files
- `install.sh`
- `scripts/smoke-test.sh`
- `source/src/story_automator/core/workflow_paths.py`
- `source/src/story_automator/commands/tmux.py`
- `source/src/story_automator/commands/basic.py`
- `payload/.claude/skills/bmad-story-automator/SKILL.md`
- `README.md`
- `CONTRIBUTING.md`

### QA Notes
- N/A

## 260412-04:50:44 - [LITE] Close review gaps in skill migration

### Summary
Closed installer, runtime, review-scope, and smoke-test gaps found after the pure-skill migration landed.

### Changed
- Tightened installer cleanup so legacy BMAD/TEA command shims are removed only when they still target old workflow-root installs.
- Required dependency skills now must provide both `SKILL.md` and a workflow entrypoint before install succeeds.
- Optional `bmad-qa-generate-e2e-tests` now resolves only when both skill and workflow files exist.
- Restored the full retrospective YOLO/doc-sync prompt contract and updated helper docs to use `scripts/story-automator`.

### Fixed
- Fixed the review skill so `.claude/skills/` source files are no longer blanket-excluded.
- Fixed runtime auto-workflow path resolution so partial QA installs do not leak missing skill references into generated prompts.
- Fixed smoke coverage for legacy wrapper cleanup, repointed-wrapper preservation, required skill half-states, and partial optional-QA states.

### Files
- `install.sh`
- `scripts/smoke-test.sh`
- `source/src/story_automator/commands/tmux.py`
- `source/src/story_automator/core/workflow_paths.py`
- `payload/.claude/skills/bmad-story-automator-review/instructions.xml`
- `payload/.claude/skills/bmad-story-automator/data/scripts-reference.md`
- `payload/.claude/skills/bmad-story-automator/data/monitoring-pattern.md`

### QA Notes
- N/A
