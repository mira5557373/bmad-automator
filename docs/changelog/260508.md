# Changelog - 260508

## 260508-07:58:11 - [LITE] Align Claude plugin marketplace metadata

### Summary
Structured the repo as an Anthropic Claude Code plugin and local marketplace catalog.

### Added
- Added `.claude-plugin/marketplace.json` for local marketplace installation.

### Changed
- Expanded Claude plugin manifest metadata with schema, homepage, repository, keywords, and `bma-d` owner attribution.
- Documented the Claude plugin root layout and local plugin/marketplace test commands.
- Ignored Firecrawl scrape cache output.

### Files
- `.claude-plugin/plugin.json`
- `.claude-plugin/marketplace.json`
- `.gitignore`
- `README.md`

### QA Notes
- N/A

## 260508-01:22:06 - [LITE] Publish refreshed npx installer

### Summary
Prepared `bmad-story-automator@1.14.2` so npx installs the new self-contained skill layout.

### Changed
- Bumped npm package metadata to `1.14.2`.
- Bumped Claude plugin metadata to `1.14.2`.
- Bumped embedded helper package metadata to `1.14.2`.

### Files
- `package.json`
- `.claude-plugin/plugin.json`
- `skills/bmad-story-automator/pyproject.toml`

### QA Notes
- N/A

## 260508-01:17:06 - [LITE] Repackage automator as self-contained skills

### Summary
Restructured Story Automator so the root `skills/` tree is canonical, copyable, and plugin-ready.

### Added
- Added `.claude-plugin/plugin.json` for Claude Code plugin loading.
- Added root `skills/bmad-story-automator` and `skills/bmad-story-automator-review` as the installable skill folders.

### Changed
- Moved the Python helper runtime into `skills/bmad-story-automator`.
- Simplified `install.sh` to copy self-contained skills instead of assembling payload and source trees.
- Updated npm packaging, docs, and tests for the new root skill layout.
- Excluded Python bytecode and cache files from packed npm output.

### Removed
- Removed the old installer-only `payload/` and separate `source/` package layout.

### Files
- `.claude-plugin/plugin.json`
- `.gitignore`
- `.npmignore`
- `README.md`
- `docs/development.md`
- `docs/installation-and-layout.md`
- `docs/plans/json-settings/`
- `install.sh`
- `package.json`
- `skills/`
- `tests/`

### QA Notes
- N/A
