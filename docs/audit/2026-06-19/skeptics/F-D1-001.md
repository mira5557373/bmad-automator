verdict: SURVIVES
reasoning: The finding is empirically true. `_accumulate_buckets` (calibration.py:96) hard-filters to `isinstance(event, (StoryCompleted, StoryFailed))` only, so events like CostCharged (which has `model`) are never inspected. The production dataclasses in telemetry_events.py:142-167 declare StoryCompleted with fields {epic, story_key, duration_s, cost_usd, tokens_in, tokens_out, attempts} and StoryFailed with {epic, story_key, error_class, reason, attempts, final_session} — neither has `model_id` or `task_kind`. The isinstance-string guard at calibration.py:100-101 therefore `continue`s on every real event, and `entries` is always `{}`. The test suite confirms this is a known production gap: tests/_calibration_fixtures.py:136-144 ships an `_ExtendedEventShim` whose docstring explicitly states "M01 is the right place to add `model_id` and `task_kind` to the event dataclasses; until then the production aggregator reads them defensively via getattr" — i.e., the tests monkey-patch widened subclasses into the registry to make calibration appear to work. The downstream `lookup_success_rate` returns the 0.5 default for every key (calibration.py:172), and `compute_drift` (drift_detector.py:108-122) sees keys() = {} | {} = {} → empty DriftReport — so `sw calibration` and `sw drift` produce silently-empty outputs in production rather than failing loudly. Test suite has zero production-path tests that build a non-empty table without the shim.
re-run_repro: |
  $ python3 -c "..." (the exact reproducer)
  entries: {}
  total_events_scanned: 1

  Additional sanity check with a CostCharged event added (which has a `model` field):
  entries: {}
  total_events_scanned: 2
  Confirms the aggregator's StoryCompleted/StoryFailed-only isinstance gate also skips CostCharged.
