verdict: REFUTED-downgrade-severity
reasoning: The mechanical claim is correct — `parse_event` at telemetry_events.py:307 calls `json.loads(line)` which raises `json.JSONDecodeError` on `not-valid-json`, so the generator at telemetry_reader.py:29-37 terminates and any subsequent lines are not yielded in that iteration. However the finding mischaracterises both intent and impact. (1) The "loud, not silent" behaviour is an explicitly documented contract: tests/test_telemetry_reader.py:55-62 (`test_malformed_line_propagates_json_decode_error`) asserts the JSONDecodeError must propagate, with the comment "REQ-07: corruption is loud, not silent." failure_triage.py:122 reiterates "does not catch the underlying iterator's exceptions (they propagate verbatim per REQ-12)." (2) The only end-user command-level consumer, commands/telemetry_report.py:98-107, already wraps iteration in `except (ValueError, TypeError)` (json.JSONDecodeError is a ValueError subclass) and returns a structured `{"ok": false, "error": "corrupt_telemetry", "detail": ...}` payload — there is no "silent loss"; the operator sees the corruption immediately. (3) Under the stated trust contract (single-writer telemetry_emitter.py guarded by `filelock.FileLock`, append-only JSONL, may be torn on ENOSPC/crash), the realistic corruption mode is a torn LAST line — by definition no later events exist to "lose." Mid-file corruption requires non-trivial external tampering of a TRUSTED file. The finding's "silently loses every event after it" claim is therefore inaccurate, and the realistic impact under the trust model is "the report command aborts with a clean error" rather than data loss. Downgrade severity from must to should (the reader could yield-and-continue on per-line errors for resilience, but the current behaviour is intentional, contract-tested, and properly handled by the only command caller).
re-run_repro: RAISED: JSONDecodeError Expecting value: line 1 column 1 (char 0)  — third event was not yielded, exactly as the finding predicts mechanically.
