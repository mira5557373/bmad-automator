# D8 — Findings: Test coverage + Hypothesis property tests

Branch: `bma-d/integration-all`. Coverage was successfully measured with `pytest-cov` 7.1.0 / `coverage` 7.14.1. Hypothesis 6.155.5 is installed but **not used in any test under `tests/`**.

Out of scope (per prompt): mutation testing, atheris fuzz harnesses. Already-fixed (do not re-flag): telemetry event handling, audit chain validation, atomic_io stale check.

---

## F-D8-001
- severity: should
- likelihood: high
- title: commands/agent_config_cmd.py at 8% line coverage (floor 75%, −67 pp)
- file: skills/bmad-story-automator/src/story_automator/commands/agent_config_cmd.py:1
- excerpt: |
    module: commands/agent_config_cmd.py — line 8%, branch 6%, floor 75%/70%
    coverage.py reports lines 10-72, 76-84 entirely uncovered
    (the whole `cmd_agent_config` dispatch + `_flag_map` helper)
- repro: |
    source .venv/bin/activate
    python -m pytest tests/ --cov=story_automator --cov-branch --cov-report=term-missing -q 2>&1 \
      | grep agent_config_cmd
- proposed_fix: Add `tests/test_agent_config_cmd.py` exercising each subcommand (`list`, `add`, `remove`, missing-arg paths) — the file is 77 stmts, so a small example-based table-driven test gets it to floor quickly.
- effort: S
- confidence: 5

## F-D8-002
- severity: should
- likelihood: high
- title: commands/basic.py at 45% line coverage (floor 75%, −30 pp)
- file: skills/bmad-story-automator/src/story_automator/commands/basic.py:1
- excerpt: |
    module: commands/basic.py — line 45%, branch 39%, floor 75%/70%
    uncovered ranges include 52-60, 64-87, 186-220, 224-244 (most of init/cleanup paths)
- repro: |
    grep basic.py /tmp/audit-lite/coverage-branch.json
    # or: python -m pytest tests/ --cov=story_automator --cov-branch -q 2>&1 | grep basic.py
- proposed_fix: Add focused unit tests for each `cmd_*` entry in `basic.py` covering happy-path and missing-arg branches; many functions are thin CLI wrappers, so a small fixture-driven suite would land most of the gap.
- effort: M
- confidence: 5

## F-D8-003
- severity: should
- likelihood: medium
- title: core/common.py at 58% line / 50% branch (floor 80%/75%, −22 / −25 pp)
- file: skills/bmad-story-automator/src/story_automator/core/common.py:1
- excerpt: |
    module: core/common.py — line 58%, branch 50%, floor 80%/75%
    uncovered: 40-43, 89, 93, 105, 109-122, 126, 134-150, 154-162, 166-175, 179, 183, 198
    (multiple helper utilities — print_json variants, command runners, redaction paths)
- repro: |
    grep common.py /tmp/audit-lite/coverage-branch.json | head -3
- proposed_fix: This module is a shared utility surface — write targeted tests in `tests/test_common.py` for each exported helper (especially the subprocess-runner branches and redactor paths), or remove dead helpers if any prove unused.
- effort: M
- confidence: 4

## F-D8-004
- severity: should
- likelihood: medium
- title: core/tmux_runtime.py at 62% line / 57% branch (floor 80%/75%, −18 pp)
- file: skills/bmad-story-automator/src/story_automator/core/tmux_runtime.py:1
- excerpt: |
    module: core/tmux_runtime.py — 783 stmts, line 62%, branch 57%
    uncovered ranges include 391-413, 1022-1077, 1093-1219 — large error/recovery paths
- repro: |
    grep tmux_runtime.py /tmp/audit-lite/coverage-branch.json | head -3
- proposed_fix: Many uncovered branches are tmux-error and timeout recovery paths. Mock the tmux adapter (or use the existing fake in `tests/test_tmux_integration.py`) to drive injected failures. Aim for the ~150 missing lines in error recovery to get to floor.
- effort: L
- confidence: 4

## F-D8-005
- severity: should
- likelihood: medium
- title: commands/tmux.py at 62% line / 59% branch (floor 75%/70%, −13 / −11 pp)
- file: skills/bmad-story-automator/src/story_automator/commands/tmux.py:1
- excerpt: |
    module: commands/tmux.py — line 62%, branch 59%, floor 75%/70%
    big uncovered block 68-131 (per-action dispatch), 158-183, 266-305 (session lifecycle)
- repro: |
    grep -E "commands/tmux\.py" /tmp/audit-lite/coverage-branch.json | head -3
- proposed_fix: Extend `tests/test_tmux_integration.py` to cover the missing subcommand branches (the CLI dispatcher); use a recorded tmux fake to keep tests hermetic.
- effort: M
- confidence: 4

## F-D8-006
- severity: should
- likelihood: medium
- title: commands/orchestrator.py at 65% line / 62% branch (floor 75%/70%, −10 / −8 pp)
- file: skills/bmad-story-automator/src/story_automator/commands/orchestrator.py:1
- excerpt: |
    module: commands/orchestrator.py — 450 stmts, line 65%, branch 62%
    uncovered: 127-238 (option-parsing + dispatch), 391-449 (loop body fallbacks)
- repro: |
    grep -E "commands/orchestrator\.py" /tmp/audit-lite/coverage-branch.json | head -3
- proposed_fix: This is borderline (−10pp). Add a `tests/test_orchestrator_cli_dispatch.py` covering the missing subcommand/flag-parsing branches; the loop logic itself is exercised by `test_orchestration_loop.py`.
- effort: M
- confidence: 4

## F-D8-007
- severity: should
- likelihood: high
- title: No hypothesis state-machine test for core/atomic_io (integrity-critical)
- file: tests/test_atomic_io.py:1 (or new tests/test_atomic_io_hypothesis.py)
- excerpt: |
    rg -n 'from hypothesis|RuleBasedStateMachine' tests/  →  zero matches
    tests/test_atomic_io.py tests only example-based concurrency (one thread + simple permission retries)
    Lock-payload semantics in core/atomic_io.py:200-360 (acquire/release/heartbeat/foreign_host/pid_recycle) are
    state-rich and the parent module flagged this as SHOULD-high per the D8 rubric.
- repro: |
    rg -n 'from hypothesis|@given|RuleBasedStateMachine' tests/
    # expected: zero matches
- proposed_fix: Add a Hypothesis `RuleBasedStateMachine` modeling two acquirer processes with rules `{acquire, release, heartbeat, crash, reclaim, foreign_host, pid_recycle}` and an invariant that no two live processes hold the lock simultaneously and that a foreign-host or pid-recycle payload never grants reclaim without timeout.
- effort: M
- confidence: 5

## F-D8-008
- severity: should
- likelihood: high
- title: No hypothesis state-machine test for core/audit append+verify (integrity-critical)
- file: tests/test_audit_append.py:1 / tests/test_audit_verify.py:1 (or new tests/test_audit_hypothesis.py)
- excerpt: |
    audit chain in core/audit.py is hash-linked JSONL; current tests cover example mutations
    (test_audit_verify.py:197-457). No randomised state machine over
    {append, verify, truncate, splice} sequences exists.
- repro: |
    rg -n 'RuleBasedStateMachine|@rule' tests/test_audit_append.py tests/test_audit_verify.py
    # expected: zero matches
- proposed_fix: Add a `RuleBasedStateMachine` with rules `{append(event), verify(), truncate(n), splice(n, payload)}` and an invariant that `verify()` returns `(True, len)` iff no truncation or splice has occurred since the last clean append, and returns `(False, k)` pointing at the last clean seq otherwise.
- effort: M
- confidence: 5

## F-D8-009
- severity: should
- likelihood: medium
- title: No hypothesis round-trip for telemetry_events.parse_event on adversarial text
- file: tests/test_telemetry_events.py:454 (or new tests/test_telemetry_events_hypothesis.py)
- excerpt: |
    tests/test_telemetry_events.py:456-611 enumerates literal parse_event cases but lacks a
    Hypothesis strategy for text containing `\n \r \t \x00`, surrogate halves,
    NFC/NFKC pairs, and 4-byte UTF-8 (emoji + supplementary plane).
- repro: |
    rg -n 'from hypothesis|st\.text|st\.characters' tests/test_telemetry_events.py
    # expected: zero matches
- proposed_fix: Add `@given(st.text(alphabet=st.characters(blacklist_categories=()), min_size=0, max_size=200))` and assert `parse_event(compact_json(event))` round-trips for every concrete event class (the existing REQ-08 invariant test at line 687 covers structured cases only).
- effort: S
- confidence: 5

## F-D8-010
- severity: should
- likelihood: medium
- title: No hypothesis adversarial-input strategy for frontmatter.parse / epic_parser.parse
- file: tests/test_frontmatter.py:1 / tests/test_epic_parser.py:1
- excerpt: |
    frontmatter.parse handles inline+multiline YAML-ish lists and is exposed to user-authored md;
    epic_parser.parse drives story discovery. Neither has a strategy-based test for malformed
    inputs (billion-laughs, alias bombs, BOM, CRLF mix, duplicate keys) or for the
    serialize-parse-serialize idempotence property.
- repro: |
    rg -n 'from hypothesis|@given' tests/test_frontmatter.py tests/test_epic_parser.py
    # expected: zero matches
- proposed_fix: For `frontmatter.parse`, add `@given(st.text())` plus targeted bytes inputs (BOM-prefixed, CRLF-mixed) and assert no crash + that idempotent re-parse produces the same dict. For `epic_parser.parse`, add an idempotence property: `parse(serialize(parse(s))) == parse(s)` over generated story-block text.
- effort: M
- confidence: 4
