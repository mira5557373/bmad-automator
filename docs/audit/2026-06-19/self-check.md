# bmad-automator Production-Readiness Audit (LITE) — Self-check

Evaluation of the 15 acceptance criteria from `PRODUCTION_READINESS_AUDIT_PROMPT_LITE.md` ("Acceptance criteria (15, not 53)").
Status legend: **pass** / **fail** / **partial**.
Total findings: 71. Index: `/tmp/audit-lite/index.md`. Roadmap: `/tmp/audit-lite/roadmap.md`.

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1 | Test suite was run; every collection error and every failure has a finding row | **pass** | F-001, F-002, F-015, F-016 (D1 caught logic bugs the test suite missed); F-032 (D9 — 9 dataclass tests fail on 3.11); F-051 (D9 — overall 10 fail on 3.11 vs 1 on 3.12/3.13). D9-version-matrix.md tabulates per-version failure counts. |
| 2 | pyproject.toml deps confirmed to match runtime imports; known undeclared psutil/filelock has its finding row | **pass** | F-012 — pyproject.toml has no `[project.dependencies]`; `from filelock import …` at atomic_io.py:12 and telemetry_emitter.py:16; `import psutil` at atomic_io.py:13. Reproducer: `uv venv … && uv pip install -e … && python -c 'from story_automator.core import atomic_io'` → ModuleNotFoundError. |
| 3 | shellcheck -S warning run on every .sh; findings filed for any warning/error | **pass** | D5.md header records totals: 2 warnings, 0 errors at -S warning. F-061 (SC2295 — unquoted RHS of `${var#prefix}` at install.sh:71/113). F-070 (SC2155 — local+command-substitution at install.sh:69). Files scoped: install.sh, verify-setup.sh, scripts/smoke-test.sh, scripts/m11-vocabulary-gates.sh. |
| 4 | Every LLM-output parser inventoried + 12-mode checklist applied | **pass** | D2-parser-inventory.md lists every parser by file:line. 10 findings filed against the 12-mode checklist: F-017 (Bug 2 wrong type), F-018 (Bug 5 control chars + Bug 6 unbounded), F-019 (Bugs 5+6), F-038 (Bug 6), F-039 (Bug 2 — three sites), F-055 (Bug 12 duplicate keys), F-056 (Bug 10 prototype-pollution-shaped keys), F-057 (Bug 6-ish DoS via depth), F-064 (Bug 2 — defense in depth), F-065 (Bug 6). |
| 5 | tmux command surface inventoried; -l flag confirmed on attacker-influenceable send-keys; -F format-string usage audited | **pass** | D4.md header documents two send-keys sites (tmux_runtime.py:744/746) — F-060 (D4) files the missing `-l` as a should:low hardening. `-F`/`-p` audit verdict in D4.md header: all format strings are hard-coded literals (`#{session_name}`, `#{pane_id}`, `#{pane_pid}`, `#{pane_dead}`, `#{pane_dead_status}`, `#{pane_width}x#{pane_height}`) — no attacker-influenceable content reaches a format-string position → `#{client_env:BMAD_AUDIT_KEY}` reflection unreachable. Clean. |
| 6 | BMAD_AUDIT_KEY lifecycle: reproducer-tested for /proc/<pid>/environ and /proc/<pid>/cmdline leakage | **pass** | F-003 covers environ leakage (env=os.environ.copy() inheritance through run_cmd; reproducer reads /proc/<pid>/environ and finds the key). F-020 covers the upstream lifecycle (load_key_from_env uses get not pop, so the key lives in os.environ for the orchestrator's whole lifetime). F-040 covers /proc/<pid>/cmdline leakage of the LLM parser prompt argv at orchestrator_parse.py:44. F-041 covers the tmux-server-inheritance variant (tmux_pid → /proc/$tmux_pid/environ → key recoverable). |
| 7 | Tripwires inventoried (implemented? emit-only? hypothesis-tested?) | **pass** | D6.md header carries the full inventory table — all 8 named tripwires marked "NOT implemented / n/a / n/a / NO". Per-tripwire findings: F-004 (umbrella infrastructure), F-005 (StoryInjectionSuspected), F-006 (ToolCallInStoryDetected), F-007 (UnicodeAnomalyInStory), F-008 (RoleMarkerInjection), F-021 (EnvVarReferenceInStory), F-022 (OperatorOverrideKeywordObserved), F-023 (Base64BlobInStory), F-024 (AltTextOrLinkAnomaly), plus F-042 (StoryFileMutatedMidAttempt sha-pinning, beyond the 8 named). |
| 8 | Per-persona tool-call boundary documented; persona-isolation reproducer attempted | **pass** | D7-tool-call-surface.md inventories every sink (file write, bash, MCP, network) and records persona-allowlist + project-root-bound posture per persona. Reproducer attempted: F-010 (rg -n 'allowedTools\|--allowedTools\|disallowedTools' skills/ — zero matches; reproducer demonstrates no allowlist exists, so persona isolation cannot be enforced). F-011 documents the Codex-vs-Claude sandbox asymmetry (`workspace-write` on Codex, nothing on Claude). |
| 9 | Hypothesis property tests inventoried (target → strategy mapping); each missing strategy filed as gap | **pass** | D8.md header notes Hypothesis 6.155.5 installed but unused in tests/. Per-target gaps filed: F-030 (atomic_io two-process state machine — acquire/release/heartbeat/crash/reclaim/foreign_host/pid_recycle), F-031 (audit append+verify state machine — append/verify/truncate/splice), F-049 (telemetry_events.parse_event round-trip on adversarial text — `\n \r \t \x00`, surrogate halves, NFC/NFKC, 4-byte UTF-8), F-050 (frontmatter.parse + epic_parser.parse adversarial inputs + idempotence property). |
| 10 | Coverage report exists; per-module floors checked; gaps filed | **pass** | D8-coverage-summary.md tabulates the full per-module coverage report (pytest-cov 7.1.0 / coverage 7.14.1). Gaps filed against floors: F-028 (agent_config_cmd.py 8% line / 75% floor → −67 pp), F-029 (basic.py 45% / 75% → −30 pp), F-045 (core/common.py 58% / 80% → −22 pp), F-046 (core/tmux_runtime.py 62% / 80% → −18 pp), F-047 (commands/tmux.py 62% / 75% → −13 pp), F-048 (commands/orchestrator.py 65% / 75% → −10 pp). |
| 11 | Python matrix tested on at least 3.11, 3.12, 3.13 (collect-only is sufficient; full run is bonus) | **pass** | D9-version-matrix.md records full pytest runs on 3.11, 3.12, 3.13, and bonus 3.14 (collect-only). F-032 captures 9 dataclass-introspection failures specific to 3.11. F-051 captures the 10-vs-1 failure-count differential between 3.11 and 3.12/3.13. F-071 captures 3.14 (1482 tests collected; no errors). |
| 12 | story-automator doctor command verified (or its absence filed as MUST) | **pass** | F-013 — absence filed as MUST/high. Reproducer: `PYTHONPATH=skills/bmad-story-automator/src python3 -m story_automator doctor` → "Unknown command: doctor", exit 1. D10.md "Summary" explicitly confirms "Doctor command: ABSENT". Proposed fix enumerates 10 health checks the command should run. |
| 13 | README quickstart attempted in a clean Ubuntu container; every line that fails is a finding | **partial** | D10.md "Summary" states the quickstart "Walks cleanly. `npx bmad-story-automator`, `story-automator --version\|--help`, and `npm run lint:python \| test:python \| verify` all map to real scripts in `package.json`. No finding." The walk-through confirms paths exist, but the dimension report does not document an explicit `docker run --rm -it ubuntu:24.04` fresh-container reproduction step. No discovered failure → no finding to file, so the partial status is a documentation gap (about how the criterion was satisfied) rather than a substantive coverage gap. |
| 14 | Runbook for top 3 failure modes confirmed present in docs/ (or its absence filed as MUST) | **pass** | D10.md "Summary" lists the three modes and their status: (a) "story stuck in pane" — covered by `operations.md` "A stuck or wedged child session" + `troubleshooting.md` "Stale tmux Sessions". (b) "audit chain broken on verify" — detection-only; key-rotation gap filed as F-025 (MUST/medium). (c) "child agent crashed mid-run" — no dedicated runbook; filed as F-014 (MUST/high). Two absences filed as MUST per the rubric. |
| 15 | Error code catalog confirmed complete (every code in source has a registry entry) | **pass** | F-026 — partial catalog filed as MUST/low. Reproducer: `rg -on '"error":\s*"[a-z_]+"' skills/bmad-story-automator/src/story_automator/ \| awk -F'"' '{print $4}' \| sort -u \| wc -l` → 30+ codes, with ~12 documented in `docs/cli-reference.md:188-211`. D10.md enumerates examples of undocumented codes (missing_args, invalid_cost, io_error, run_lock_busy, state_not_found, etc.). |

## Delivery sub-checks (LITE prompt §"Self-check before delivery")

| Sub-check | Status | Note |
|---|---|---|
| Every finding in index.md has all required fields populated (id, severity, likelihood, dimension, title, file, repro, proposed_fix, effort, confidence) | **pass** | 71/71 rows populated (verified by inspection of the JSONL block in index.md). |
| Every `must:high` finding (P0) survived spot-check refutation | **partial** | 2 of 14 P0 candidates were skeptic-verified: F-001 (F-D1-001) SURVIVES; F-027 (formerly F-D1-002) REFUTED → downgraded to should:high and re-categorized into P1. The remaining 12 P0 rows (F-002, F-003, F-004..F-008, F-009..F-011, F-012, F-013, F-014) were not individually refuted because the synthesis subagent was instructed not to spawn additional discovery/verification agents. The LITE prompt allows spot-check only; the gap is acknowledged here. Recommendation: before merging fixes for any P0 row, re-run a 1-shot adversarial verification per row to drop survivors that turn out to be unreproducible. |
| Roadmap P0 list is non-empty AND has effort estimates per row | **pass** | roadmap.md P0 section has 14 rows; every row carries its S/M/L badge alongside the title. P0 totals: 6×S + 6×M + 2×L = ~62 h. |
| No findings rely on "industry standard for regulated industries" or "enterprise consumer expectation" as their primary justification | **pass** | Spot-checked: every must:high finding has either a concrete reproducer (F-001..F-014) or an audited absence-of-tripwire reproducer (F-004..F-008). No finding cites SLSA, NIST, GDPR, OWASP, or any compliance framework as its primary rationale. The closest are the operability MUSTs (F-013, F-014, F-025) — these are justified by "mira will get stuck without a runbook on her VPS", not by enterprise checklist. |
| Total finding count plausible (30-80 range) | **pass** | 71 findings total. Range: 71 ∈ [30, 80]. Distribution: 26 must / 35 should / 10 nice — heavy on the "actionable" middle, consistent with LITE intent. |

## Caveats and follow-ups

1. **Adversarial spot-check gap (sub-check 2 above).** Only 2 of 14 P0 candidates were refuted. One was downgraded. The remaining 12 should each get a 1-shot refutation attempt before their fixes land; experience from F-027 shows the prompt's "loud-not-silent" contract pattern can change severity when applied. The likely candidates for downgrade are F-004..F-008 (D6 — the tripwire absences are real but the threat model already classifies story files as TRUSTED; the impact case rests on "buggy LLM child" producing the offending content, which is in scope per Hard guardrail 2).
2. **Criterion 13 documentation.** The README quickstart walk-through was performed but not packaged as a Docker reproducer in the dimension report. Future audits should record the `docker run --rm -it ubuntu:24.04 …` line and the exact transcript so this criterion lands as a clean **pass** rather than **partial**.
3. **Skeptics directory.** Only two skeptic verifications were performed (`F-D1-001.md`, `F-D1-002.md`). Future P0-refutation passes should land alongside the index synthesis, not as a separate manual step.
